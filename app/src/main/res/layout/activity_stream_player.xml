<?xml version="1.0" encoding="utf-8"?>
<!--
  Unified stream player layout.
  - videoSurface (FrameLayout): visible for VIDEO, gone for AUDIO
  - audioPanel   (LinearLayout): visible for AUDIO, gone for VIDEO
  - playerView (PlayerView): always present, surface_type="none" for audio so
    ExoPlayer controls render without a video surface when in audio mode.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark">

    <!-- ══ VIDEO surface (full-screen ExoPlayer view) ══════════════════════ -->
    <FrameLayout
        android:id="@+id/videoSurface"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/black"
        android:visibility="gone">

        <androidx.media3.ui.PlayerView
            android:id="@+id/playerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:use_controller="true"
            app:controller_layout_id="@layout/custom_player_controls"
            app:show_buffering="when_playing"
            app:resize_mode="fit"
            app:auto_show="true"
            app:hide_on_touch="false"
            app:show_timeout="0" />

        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_gravity="center"
            android:indeterminateTint="@color/accent"
            android:visibility="gone" />
    </FrameLayout>

    <!-- ══ AUDIO panel (artwork + info + controls) ═════════════════════════ -->
    <LinearLayout
        android:id="@+id/audioPanel"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/bg_dark"
        android:visibility="gone">

        <!-- Album art -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center">

            <TextView
                android:layout_width="180dp"
                android:layout_height="180dp"
                android:layout_gravity="center"
                android:gravity="center"
                android:text="&#127925;"
                android:textSize="80sp"
                android:background="@drawable/bg_album_art" />
        </FrameLayout>

        <!-- Track info -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="24dp"
            android:paddingTop="16dp">

            <TextView
                android:id="@+id/streamTitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/white"
                android:textSize="20sp"
                android:textStyle="bold"
                android:gravity="center"
                android:maxLines="2"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/streamDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/hint_color"
                android:textSize="14sp"
                android:gravity="center"
                android:layout_marginTop="4dp"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/statusText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/accent"
                android:textSize="14sp"
                android:gravity="center"
                android:layout_marginTop="8dp"
                android:text="@string/status_connecting" />
        </LinearLayout>

        <!-- Audio transport controls via a dedicated PlayerView (no surface) -->
        <androidx.media3.ui.PlayerView
            android:id="@+id/audioControlsView"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:background="@color/surface_dark"
            app:use_controller="true"
            app:show_buffering="never"
            app:surface_type="none"
            app:controller_layout_id="@layout/audio_controls"
            app:auto_show="true"
            app:hide_on_touch="false"
            app:show_timeout="0" />

    </LinearLayout>

    <!-- ══ Top overlay: back + title + PiP (always on top) ════════════════ -->
    <LinearLayout
        android:id="@+id/topControls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingLeft="12dp"
        android:paddingRight="12dp"
        android:paddingBottom="12dp"
        android:paddingTop="0dp"
        android:gravity="center_vertical"
        android:background="@drawable/gradient_top">

        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_arrow_back"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/btn_back_desc"
            app:tint="@color/white" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingHorizontal="8dp">

            <!-- Title/description shown in top bar only for VIDEO;
                 audio mode has its own larger title block below -->
            <TextView
                android:id="@+id/videoTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/white"
                android:textSize="16sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/videoDescription"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#AAFFFFFF"
                android:textSize="12sp"
                android:maxLines="1"
                android:ellipsize="end" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/btnPip"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_pip"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/btn_pip_desc"
            app:tint="@color/white"
            android:visibility="gone" />
    </LinearLayout>

</FrameLayout>
